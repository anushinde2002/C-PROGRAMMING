#include<stdio.h>
int main()
{
  int r,perimeter;
  printf("Enter radius of circle:");
  scanf("%d",&r);
  perimeter=2*3.142*r;
  printf("The perimeter of circle is %d:",perimeter);
  return 0;
}